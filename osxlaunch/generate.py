"""
Generate a plist file based on the variables passed.
"""


def generate(vars):
    """Generate a plist file based on the variables passed."""
    for key, value in vars.items():
        print(key)
        print(value)
